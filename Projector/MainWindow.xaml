<Window x:Class="Projector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projector"
        xmlns:ViewModels="clr-namespace:Projector.ViewModels"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Projector" Height="450" Width="800" WindowStartupLocation="CenterScreen" >
    <Window.DataContext>
        <ViewModels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding ChartTitle}" FontFamily="Consolas" FontSize="20"/>

        <Grid Grid.Row="1">
            <lvc:CartesianChart Grid.Column="1" Series="{Binding ActualChartData}" LegendLocation="Bottom" Margin="20">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="{Binding XAxisTitle}" Labels="{Binding AffectedManualWorkcenters}" LabelsRotation="{Binding XAxisRotation}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1" IsEnabled="False"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="{Binding YAxisTitle}" LabelFormatter="{Binding YAxisFormatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Grid>

    </Grid>
</Window>
